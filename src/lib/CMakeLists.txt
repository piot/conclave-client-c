# generated by cmake-generator
cmake_minimum_required(VERSION 3.16.3)

add_library(conclave-client STATIC 
  client.c
  debug.c
  incoming.c
  outgoing.c)

include(Tornado.cmake)
set_tornado(conclave-client)

target_include_directories(conclave-client PUBLIC ../include)


target_link_libraries(conclave-client PUBLIC 
  conclave-serialize
  datagram-transport
  flood
  monotonic-time
  imprint
  discoid
  secure-random
  time-tick
  datagram-reassembly)

